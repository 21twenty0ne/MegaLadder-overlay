# MegaLadder Stats Overlay v1.1.0

## 🇷🇺 Русский

### 🆕 Изменения

*   **Система Профилей**:
    *   **Новая функция**: Добавлена система управления профилями с 3 неизменяемыми стандартными профилями: `Full`, `Focus Rival` и `Full Focus`.
    *   **Пользовательские профили**: Пользователи могут сохранять свои настройки как собственные профили и удалять их.
    *   **Интерфейс**: Добавлен выпадающий список выбора профиля с кнопками **Сохранить (💾)** и **Удалить (🗑)** в панели настроек.

*   **Детальное управление статистикой**:
    *   Добавлены отдельные переключатели для **Убийств**, **Уровня** и **Сложности**.
    *   **Динамическая верстка**: В режиме `Full Focus` оверлей теперь автоматически адаптируется, заполняя пространство в зависимости от включенных характеристик (например, скрытие Убийств увеличит место для Уровня и Разницы).
    *   **Таблица статистики**: В стандартном режиме таблица статистики также учитывает эти настройки, скрывая соответствующие строки/секции.

*   **Улучшения интерфейса**:
    *   **Индикация состояния**: Настройки теперь визуально отключаются (становятся серыми), когда они принудительно заданы режимом Фокуса.
    *   **Предупреждения**: Добавлены информационные сообщения в панелях Настроек и Видимости при активных режимах Фокуса.
    *   **Доработки**:
        *   Скорректирован размер текста для стандартных профилей (`Full Focus` стал более компактным).
        *   Исправлены настройки профиля `Focus Rival` (баны отключены).
        *   Исправлен стиль и видимость выпадающего списка профилей.

*   **Настройки по умолчанию**:
    *   `Full Focus Mode` и `Focus Rival Mode` теперь отключены по умолчанию.

---

## 🇬🇧 English

### 🆕 Changes

*   **Profile System**:
    *   **New Feature**: Added a profile management system with 3 persistent default profiles: `Full`, `Focus Rival`, and `Full Focus`.
    *   **Custom Profiles**: Users can now save their own setting configurations as custom profiles and delete them.
    *   **UI**: Added a profile selector dropdown with **Save (💾)** and **Delete (🗑)** buttons in the Settings panel.

*   **Granular Stat Control**:
    *   Added individual toggles for **Kills**, **Level**, and **Diff**.
    *   **Dynamic Layout**: In `Full Focus Mode`, the overlay now automatically adjusts to fill the space based on which stats are enabled (e.g., hiding Kills will expand Level and Diff).
    *   **Stats Table**: In standard mode, the statistics table also respects these toggles, hiding rows/sections as needed.

*   **UI Improvements**:
    *   **Status Indicators**: Settings are now visually disabled (greyed out) when they are enforced by a Focus mode.
    *   **Warnings**: Added informative flags in Settings and Visibility panels when Focus modes are active.
    *   **Refinements**: 
        *   Adjusted text scaling for default profiles (`Full Focus` is more compact).
        *   Fixed `Focus Rival` default settings (bans disabled).
        *   Fixed Profile dropdown styling and visibility issues.

*   **Defaults**:
    *   `Full Focus Mode` and `Focus Rival Mode` are now disabled by default.
